# FlyPoet A/B 对照报告（v1，0.1B 中文文学模型）

**日期**：2026-09-17 至 09-18
**任务**：从 0 训练 0.1B 字符级中文文学模型（宋词+全唐诗+元曲+鲁迅全集，2.2M 训练 token，词表 7876）
**对照组**：std = 标准 GPT（d=768, 12 层, 91.4M 参数）
**实验组**：fly = 同架构 + 果蝇机制（注意力 top-10% k-WTA、FFN top-25% 稀疏化、gatedlearn 误差门控 z≥3.5）
**预算**：各 4000 步，batch 24×256 token

## 一、结果总表

| 指标 | std（标准） | fly（果蝇版） | 胜者 |
|---|---|---|---|
| 最终 val loss（4 次评估单调下降） | **5.390** | 5.887 | std |
| 最终 train loss | **5.284** | 5.791 | std |
| 训练吞吐（稳态 tok/s） | **19,582** | 18,024 | std（快 8.6%）|
| 参数量 | 91.4M | 91.4M | 平 |

**结论：本轮 std 全面胜出。果蝇机制组合（稀疏化+误差门控）在小语料短程训练上损失了约 0.5 nats 的拟合能力，且稀疏化的额外计算没有被省回来（k-WTA 的 topk 操作成本超过了 FFN 稀疏化的节省）。**

## 二、过程曲线

| step | std val | fly val | 差距 |
|---|---|---|---|
| 1000 | 6.272 | 6.377 | +0.105 |
| 2000 | 5.924 | 6.190 | +0.267 |
| 3000 | 5.647 | 6.057 | +0.411 |
| 4000 | 5.390 | 5.887 | +0.497 |

**差距随训练单调扩大**——不是"果蝇版学得慢但会赶上"的形态，而是持续落后。这与 T103（k-WTA 从头训练更优）矛盾，指向一个关键混淆变量：

## 三、最重要的发现：与 T103 结论的冲突及解释

T103 中 k-WTA 注意力带来 PPL -8%，本轮却全程落后。两个可检验的差异：

1. **训练时长**：T103 的 fly 模型训了等效 3 倍步数——稀疏约束早期吃亏、后期靠去相关反超的假设未被本轮 4000 步覆盖；
2. **机制叠加干扰**：T103 只开注意力 k-WTA；本轮同时开了 FFN top-25% + 误差门控。误差门控（z≥3.5）在语料单一（文学）时几乎不触发，但 scale_lr=True 的惊喜放大可能在小 loss 波动下引入了额外噪声。

**下一步实验（已就绪可跑）**：fly-lite 臂（只开注意力 k-WTA，去掉误差门控与 FFN 稀疏化）× 12000 步——检验"单独机制+长训练"假设。

## 四、生成质量（step 4000，"床前明月光"续写）

两模型都仍处于字符乱语阶段（2.2M token 对 0.1B 模型严重不足，T107 同款问题），无法评判文学性。std 的输出中文语法结构略多，fly 夹杂更多重复模式（"亭那"反复出现——k-WTA 稀疏化下激活的通道塌缩迹象）。

## 五、对"果蝇机制"叙事的净结论（诚实）

1. **误差门控在单任务自监督预训练中无优势**——它的主场是多任务/持续学习（T59e 本来的语境）。本轮等于又一次验证了"机制错配场景无收益"。
2. **k-WTA 稀疏化的收益条件**比 T103 报告的更窄：需要更长训练或单独使用。
3. 计算效率叙事不成立（至少在本规模）：topk 开销 > FFN 稀疏化节省。

## 六、资产

- 训练脚本 `train_flypoet.py`（含完整果蝇机制实现，可复用于任意语料）
- 两臂完整训练曲线 `logs/*_curve.jsonl`、模型权重 `logs/*_model.pt`、生成样本
- 数据管线 `prepare_data.py`（可扩展全量唐诗 133MB 语料——本轮仅用 2.2M 字符子集）

## 七、若继续加码的路线（按价值排序）

1. **fly-lite 单机制 × 12k 步**——检验 T103 复现性（约 6h GPU）
2. **全量语料**（133MB 唐诗+宋词全部）× 两臂——语料瓶颈消除后重赛
3. **持续学习阶段**——预训练后顺序学"词→诗→鲁迅"三域，这才是误差门控+隔室化（T100）的主场，预期是果蝇机制真正翻盘的场景
