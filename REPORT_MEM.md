# FlyPoet 记忆管理三部曲（2026-09-19）

接续 REPORT_V2（k-WTA = 收敛加速）之后的三个果蝇机制实验与 2026-09-20 凌晨的六个"大赌注"
实验（§十），同一 92.6M char-level trunk、`data_v2` 语料流、`decide_data` 8 域标注集
（47763/5308，CWT2 精选）。

**TL;DR**：三个机制两个成立、一个部分成立。
①意外门控持续学习：**遗忘减少 79%，四域终点全面最优**；
③主动遗忘淋浴：**无数据无梯度撤掉过拟合尾巴，且定向性一锤定音**（均匀衰减=摧毁，定向=修复）；
②熟悉度：信号存在但单窗口弱（AUC 0.59），集合级检测强（AUC 0.93）——能回答"这个评测集脏不脏"，
回答不了"这一条是训练数据"。

---

## ① 意外门控持续学习（cl_experiment.py）

**设定**：std-24k checkpoint，顺序微调 4 域（news → dialogue → law → technology），
每域 1500 步、LR 1e-4、batch 24、seq 192、无 weight decay。三臂同数据同序：

| 臂 | 机制 |
|---|---|
| ft | 普通顺序微调（遗忘基线） |
| comp | 每域只更新随机 30% 权重元素（蘑菇体小室，域间重采样） |
| fly | comp + 惊讶门控：batch loss > μ+0.25σ（EMA 噪声地板）才更新——"无意外，不写入" |

**终点 vs 基线**（val loss，基线 = 不微调的 std-24k）：

| 域（基线） | ft 终点 | comp 终点 | fly 终点 |
|---|---|---|---|
| news (3.937) | 4.592 ↑**更差** | 3.892 | **3.551** |
| dialogue (4.248) | 5.247 ↑**更差** | 4.411 | **3.991** |
| law (3.750) | 2.376 | 2.054 | **1.911** |
| technology (3.578) | 3.979 ↑**更差** | 3.352 | **3.121** |
| 平均改进 | **−0.170** | +0.451 | **+0.735** |

**遗忘量**（该域刚训完 → 四域训完的损失回升）：ft **0.692** / comp 0.380 / **fly 0.143**。

**结论**：普通微调是双输——新域过拟合（technology 训练损失压到 1.05 但 val 反升到 3.98），
旧域被摧毁（news/dialogue 终点比不微调还差）。fly 臂唯一在全部四域同时优于基线，
遗忘量比 ft 少 **79%**。comp 单独用不够：随机小室挖得深（law 曾到 1.557）忘得也多；
门控挡住了低惊讶期的过度写入，是遗忘保护的主要来源。

**Sparse-trunk 对照（裁决"是稀疏激活还是写入管理在起作用"）**：
把同样的 ft 顺序微调协议搬到 flynetS_adaptive trunk（稀疏前向，无任何门控）上：
遗忘量 **0.674**（std trunk 0.692），终点 news 4.654 / dialogue 5.317 / law 2.306 / technology 4.046，
平均改进 **−0.203**——与 std trunk 的灾难性遗忘画面相同。
**结论：减少遗忘的是果蝇式写入管理（小室 + 惊讶门控），不是稀疏激活本身。**
"稀疏编码降低记忆干扰"（arXiv 2107.07617 的原始假设）在本规模/协议下**不成立**；
起作用的机制是"无意外不写入 + 参数分区"。

**诚实标注**：门控本质是逐 batch 的惊讶自适应早停——这正是"无意外不写入"的 ML 直译，
但要明说；单 seed、每域评估 320 窗（采样噪声 ±0.02）；域顺序只测了一条。

## ② 熟悉度电路（fam_heads.py / fam_nll.py）

果蝇 ON/OFF 新颖度探测器只答"见过没见过"。ML 版：判别 trunk 见过的窗口（train.bin）
vs 没见过的（val.bin）。

| 探针 | std | flynetS |
|---|---|---|
| 隐状态线性头（末位 hidden→logistic） | AUC 0.517（≈瞎猜） | 0.525 |
| 单窗口 NLL（score = −loss） | **AUC 0.587** | 0.598 |
| 集合级 NLL（64 窗均值） | **AUC 0.93** | 0.93+ |
| 单窗口 NLL：train 3.534 vs val 3.723 | 差 0.19 | 差 0.22 |

**污染检测召回**（把 p 比例的训练窗口混入 1000 个 val 窗口，按 −NLL 排序取 top-p）：
p=5% 召回 0.10（2× 随机）、p=50% 召回 0.54（≈随机）。

**结论（部分成立，如实报告）**：记忆签名真实存在——92.6M 小模型训 ~5.6 epoch 后，
见过/没见过的 NLL 差 0.19，集合级检测近乎确定（"这个评测集有没有混训练数据"可用 ~50+ 窗
高置信回答，正是 b8"100%"事故想要的自动体检）；但**单窗口检测弱**（2× lift），
别指望它指出具体哪条脏。flynetS 的记忆签名略**强于** std（0.598 vs 0.587）——
稀疏化没有减少记忆，甚至略增。

## ③ 主动遗忘淋浴（forgetting_shower.py）

Berry et al. 2018：果蝇有专门的遗忘神经元，主动擦除是功能。钩子：三臂 24k 终点同步的
过拟合回升（std 3.610@20k → 3.673@24k）。从 std-24k 出发，500 步**无数据无梯度**的淋浴：

| 变体 | 起点 | 终点 | 最低点 |
|---|---|---|---|
| 定向衰减（\|w\| 后 30%×(1−1e−3)/步） | 3.647 | **3.605** | 3.597 |
| 定向衰减（\|w\| 后 10%，更强） | 3.651 | **3.588** | 3.594 |
| 定向噪声（低幅值集加 σ=1e−4·std） | 3.646 | 3.635 | **3.562** |
| **均匀衰减（对照，全部权重）** | 3.647 | **6.601（毁灭）** | — |

**【2026-09-21 撤回】本节结论被固定 val 集复核推翻。**
固定窗口确定性评估（shower_verify.py）显示：decay10/noise30 在 4 个基座
（std-24k / std-48k / k25-48k / adaptive-24k，各自正确配置前向）上 delta 全部
≈ 0（最大 0.0021）——**淋浴在任何基座上都没有可测效果**。用同一实现当场对质：
随机评估协议在同一未变函数上摆动 ±0.06（3.60↔3.72），原"3.65→3.588 恢复"
完全是评估噪声。均匀衰减→6.60 的毁灭性结论保留（超噪声 100 倍，且物理直白：
全部权重 ×0.61），但它只是"缩水一半的权重当然坏"，**不构成对果蝇式主动遗忘机制
的任何支持**。方法论教训：±0.06 噪声的评估协议测不出 0.05 以下效应——仓库内
所有 <0.05 的 val 差异解读均已按此重新过筛（12k 三臂差 0.02 属边缘，U 形两端
与 3-seed 复现的大效应不受影响）。

## 方法细节与复现

```bash
python cl_experiment.py ft 1500      # comp / fly 同理
python fam_heads.py                  # 线性头（负结果）
python fam_nll.py                    # NLL 熟悉度 + 污染曲线
python forgetting_shower.py          # 淋浴三变体
```

- 领域数据 `decide_data/{train,val}.jsonl`：`decide_head/build_data.py` 从 CWT2 精选
  （seed=5，确定性）重建，47763/5308 与事故前一致。
- 结果 JSON：`logs_v2/cl_{arm}_result.json`、`familiarity_nll_result.json`、
  `forgetting_shower_result.json`；曲线 `cl_{arm}_curve.jsonl`。
- 已知事故：filter-branch 剥历史时同步删掉了工作区的 decide_data/*.jsonl 与
  data/c4_modern.txt（前者已重建，后者可重下）；ft 臂首跑死于与熟悉度实验并发的
  CUDA OOM——**GPU 任务自此一律串行**。

---

## 十、六个"大赌注"实验（2026-09-20 凌晨，预注册命中判据）

同夜链产出：多 seed（dense×3 / k25×3）、能量臂×2、k25_adaptive、225M 规模对（215.8M，
d=1024/L16），以及下面六项。裁决全部按事先写下的判据执行。

### 背景裁决（链）

| 项 | 结果 | 裁决 |
|---|---|---|
| 多 seed：k25 vs dense | k25 均值 3.819±0.016 vs dense 3.875±0.027，3/3 种子逐个占优 | **甜点跨种子成立** |
| 225M 规模对 | dense 4.181 vs k25 4.320（k25 反而差 0.139） | **甜点不随规模存活，反转**——固定数据放大模型后通道冗余更值钱；甜点是"规模×数据"的函数 |
| 能量臂 e90/e75 | 3.931 / 3.962，keep 自选 0.585/0.359 | **能量准则劣于幅值 top-k**；自选预算落在 36–58%，远比甜点奢侈 |
| k25_adaptive | 3.837（精确 k25 3.819–3.827） | 自适应阈值在 PPL 上无增益（其价值在 12k 时的校准） |

### ① 稀疏码 = 记忆地址？——部分命中

k25-24k 码（final hidden top-25%）：同概念地址稳定性 **1.52×**（踩线命中 ≥1.5× 判据）；
模式补全自 0.203 vs 错配 0.153（阳性）；语义度量**阴性**（code-PMI Spearman 0.081 vs
dense 0.208）。**裁决：稀疏码像书架编号（稳定、可补全），不像书的内容（无语义）。**

### ② 睡眠循环训练——三 seed 后降级为"无差异（无害）"

每 3000 步淋浴 375 步（≈+3% 算力），逐种子配对（睡眠 − 同种子 dense）：
seed7 **+0.010**、seed8 **+0.002**、seed9 **−0.002**——均值 +0.003，跨零。
结合终点淋浴实验（确能撤过拟合尾巴）：周期淋浴**无害但非免费午餐**，
"训练加睡眠"的正收益未能跨 seed 站住。

### ③ 激活率 = 内感受——阴性

能量模型的逐 token keep 率：rate↔NLL Spearman 仅 −0.06；高损预测 AUC 0.537；
**OOD（train vs val）AUC 0.5005——零信号**，两流平均激活率同为 0.587。
模型"花钱方式"不携带记忆/难度信息（单 token 级）。

### ⑤ 单窗口记忆取证集成——阴性

四特征（NLL 0.605 / keep 0.503 / maxNLL 0.553 / bigram 0.480）→ 逻辑回归集成
**0.517，不敌单 NLL**。单窗口取证维持"弱"结论；集合级 0.93 仍是唯一可用模式。

### ④ 遗忘-重叠剂量曲线——无干净剂量关系，但门控活性成分坐实

fly 臂（门控恒开）扫小室比例（遗忘量=终值较训后峰值的回升幅度，越大越糟）：
0.1→0.093/改进 +0.728；0.3→0.143/+0.735；0.5→0.001/+0.565；**1.0（纯门控无小室）→
0.174/+0.710**。不单调（单 seed 噪声），但**纯门控已把遗忘量从 ft 的 0.69 压到 0.17**
（降 75%），小室的额外贡献在噪声内——"无意外不写入"是主要活性成分，参数分区是次要保险。
**跨 seed 复现（seed8）**：ft 遗忘量 0.691（vs seed7 0.692）、纯门控 0.171（vs 0.174）——
两个端点精确复现，④的核心对比稳固。诚实脚注：此处的"seed"只变 batch 采样顺序，
小室 mask 种子固定（1000+域序号），mask 随机性的方差未测。

**随机跳批对照（2026-09-21，对"惊讶选择性"的关键裁决）**：以相同 30% 小室、
按固定概率跳过 ~70% batch（不看不惊讶，纯掷骰子）：
遗忘 **0.130**、改进 **+0.745**——与惊讶门控（0.143/+0.735）**统计无差别**。
**裁决：门控的抗遗忘收益来自"更新得更少"（update throttling）本身，
"惊讶时才写"的选择性没有超出随机跳批的可测优势。**
"无意外不写入"降级为灵感来源；活性成分是"少更新 × 参数分区"。
（评审第 6 轮点名的关键对照，结果不利于我们的生物学叙事，如实记录。）

**学习能力的存续（顺序效应，零新增算力）**：逐域 1500 步的 val 改进——
ft：news +0.393 / dialogue **−0.045** / law +2.139 / technology **−0.401**（四域两负，学习抽风）；
fly：+0.511 / +0.432 / +2.109 / **+0.457**（四域全正，末域仍 +0.46）。
**门控不只少遗忘，它保住了"持续能学"这件事本身**——不管理记忆的模型到序列后段
已丧失学习能力，即进化停滞的 ML 形态。

### ⑥ 进化实验（6 基因型 × 2 新环境，门控+小室适应）——TTC 判据失效，排序结果有用

finance（E1）：全基因型 TTC≤100（阈值太松，天花板效应）；education（E2）：全体 1000 步
censored（阈值 0.4 太紧）。**TTC(E2)<TTC(E1) 无法裁决**——环境难度不匹配，判据设计失误。
但等压适应后的排序有信息：**k25 最终 finance val 最优（2.891），k02 最差（3.240），
dense 居中（2.933）**——预训练的 U 形在选择压下复现，"25% 基因型也是最佳适应者"。
二代选择实验（ winners 繁殖）留给后续。

### 六赌注记分板

| # | 赌注 | 裁决 |
|---|---|---|
| ① | 码=地址（稳定+补全） | **部分命中**（语义维度阴性） |
| ② | 睡眠循环 | 三 seed 后=**无差异**（+0.003±跨零，无害） |
| ③ | 激活率=内感受 | **阴性** |
| ④ | 剂量曲线 | 门控坐实为主成分；剂量关系无 |
| ⑤ | 单窗口取证集成 | **阴性** |
| ⑥ | 学习-学习迁移 | 判据失效（环境难度失衡）；基因型排序复现 U 形 |

外加两条硬边界：**甜点不随规模存活（225M 反转）**；**能量准则劣于幅值 top-k**。

---

## 十一、四项后续（2026-09-20 下午，无人值守链）

1. **门控阈值 K 扫描**：K=0.1/0.25/0.5 → 遗忘量 0.152/0.143/0.125，改进 0.731/0.735/0.743——
   响应平坦，**门控是免调旋钮**。
2. **稀疏码检索**（k25-24k，地址簿 6000/查询 1000，payload=下一字符）：
   Hamming hit@1 0.123 / hit@10 0.226；dense 余弦 0.128/0.230；频次与随机基线 0.05/0.047——
   **码可用作地址（2.5–4.8× 基线），但无优于 dense**；与 §①"索引非表示"自洽。
3. 48k 数据维度对与 336M 规模点：见 REPORT_V2 §九附录（甜点抗数据量 ×4；216M 反转为
   不稳定孤例）。

---

## 十二、新方向五项 + 移植因果裁决（2026-09-20/21）

**① 组合性（文献空白，阳性）**：80 个高频双字的码与其两字码并集的 Jaccard 是随机配对的
**1.669×**，且有近因梯度（后字 0.295 > 前字 0.151）——码不是纯随机指纹，
带轻微的组合绑定结构。

**③ 发展计时（快照 2k–12k）**：
- 跨快照码稳定性单调冻结：0.452 → 0.531 → 0.621 → 0.738 → **0.892**
  （同快照异窗口随机水平 0.152）——**地址系统随训练单调冻结**；
- 语义相关（码-PMI Spearman）全程平在 0.03，从不发展；
- 码空间均匀度恒定 0.151。
  **结论："先装门牌，永远不装修"——地址结构成熟后冻结，语义从不进码。**

**④ 因果消融（matched-fraction 对照）**：末位清零 top-25% 码通道的 KL 伤害（1.31）
是清零随机 25%（0.745）的 **1.76×**——训练出的稀疏子集**因果承载**计算。
与①合并后的精化叙事：**码是发动机的气缸（因果中心），不是图书馆的目录（无语义几何）**。

**⑤ 免标签门控（entropy gate）**：用预测熵代替 loss 作惊讶信号，K=0.25/0.5 两档
（遗忘 0.141/0.125，改进 0.737/0.743）与 loss 门控几乎逐位一致——**门控可迁移到
无目标信号的数据流**。

**精英通道普查**：胜出率 Gini——k25_s8 **0.515** / s9 **0.501** / 336M **0.567**
（均匀轮换基线 ≈0.001）——精英结构真实存在且随规模略增；跨 seed 精英 top-50 重合 **6.7%**
（两随机 50/768 子集的期望重合即 6.5%——**处于随机水平**）——集中度是普遍属性，精英名单是各家自己的。
216M 反转未获精英结构解释（待 216M 点普查）。

**弹性拨盘（25% 训练模型推理时变 k）**：向下优雅（k→10% 仅 +0.048），向上毁灭
（k=40% → 4.69，k=100% → 8.89 比乱猜差）——**单行道拨盘**：一模型多档位只许向下。

**门控的规模边界（诚实负结果）**：336M（严重欠训练基座）上 ft 改进 1.263 反超门控
0.979，遗忘相近（0.22 vs 0.20）——基座太生时一切都是惊异，门控退化为直通。
**门控保护需要过预训练的基底。**

**⑦ 8 域技能库（fly 臂走满 8 域）**：遗忘 0.167、改进 +0.582——四域结论外推到全域
成立（无 ft 对照，限定成立范围）。

### 新方向记分板

| # | 方向 | 裁决 |
|---|---|---|
| 组合性 | 大胆空白 | **阳性 1.669×** |
| 发展计时 | 大胆空白 | **单调冻结曲线 + 语义永不进码** |
| 因果消融 | 文献较拥挤 | **1.76× matched 对照命中** |
| 熵门控 | 实用化 | **与 loss 门控等价** |
| 精英普查 | 空白 | 结构真实（Gini 0.5+）、名单各家自己的 |
| 弹性拨盘 | 半空白 | **单行道**（向下可用向上崩） |
| 336M 门控 | 边界 | **需要过预训练基底** |

---

## 十三、规模阶梯与六项新分析（2026-09-21 夜，无人值守链）

### 精英通道普查（三规模 + 跨 seed）

| 模型 | 胜出率 Gini |
|---|---|
| k25 s8（92.6M） | 0.515 |
| k25 s9（92.6M） | 0.501 |
| k25（334M） | 0.567 |

集中度真实（均匀轮换基线 ≈0.001）且随规模略增；**跨 seed 精英 top-50 重合 6.7%
≈ 机会水平 6.5%**——集中是普遍属性，精英名单是各家自己的。
**216M k25（反转点）普查补齐：Gini 0.578——全部规模中最高**
（92.6M 0.501–0.515、334M 0.567）。反转点拥有最垄断的精英结构——这是第一条与
反转共现的结构线索，但为共现非因果（b16/b8 混淆仍在），因果化需重训干预。

### 跳批剂量曲线（pass rate 15/30/50/90%，30% 小室）

| 通过率 | 遗忘 | 改进 |
|---|---|---|
| 15% | 0.093 | 0.736 |
| 30%（=门控点） | 0.130 | 0.745 |
| 50% | 0.190 | 0.700 |
| 90% | 0.339 | 0.510 |

干净单调：**门控点精确落在随机跳批曲线上**——惊讶选择性无可测增量（§10 裁决的剂量版）。
s8 复现：30% 点 0.130/+0.745 逐位一致。

### 熵门控 K=0.1

遗忘 0.152 / 改进 0.730——与 loss 门控同 K（0.143/0.735）几乎一致，免标签版本再次打平。

### 概念级域检索（k25-24k 码，8 域）

Hamming hit@1 **0.325**（机会 0.125 的 2.6×）/ hit@10 0.882 / hit@50 0.997；
dense 余弦 0.342/0.881/0.997 逐点打平。**码几何编码粗粒度主题、不编码细粒度词汇语义**
（与 §① 码-PMI 0.03 一致）——正是 Hamming 预过滤+dense 重排两段式检索所需的证据。

### 规模阶梯（k25 vs dense，12k 步）

| 规模 | 协议 | dense | k25 | 差 |
|---|---|---|---|---|
| 92.6M | b24 / 74M tok | 3.906 | 3.819±0.016 | **k25 +0.087** |
| 215.8M | b16 / 393M tok | 4.181 | 4.320 | **dense +0.139（反转）** |
| 215.8M | b8 / 24.6M tok | 4.870 | 4.855 | **k25 +0.015** |
| 334M | b8 / 24.6M tok | 5.266 | 5.191 | **k25 +0.075** |
| 477.8M | b4 / 24.6M tok | 5.474 | （重跑中） | — |

**216M 反转在 batch8 协议下消失**——原反转与该点的 batch16/393M token 配置绑定，
不能归因于规模本身。诚实结论：在所有干净测点上 k25 ≥ dense；
"反转"是配置效应，其真正成因（若有）待 216M 精英普查补齐后回答。

### 0.5B 级（477.8M，d1536/L16）首测

dense 12k 步终点 5.474（24.6M token 极端饥饿，轨迹 5.90→5.61→5.47 仍陡降未收敛，
梯度范数 16–25 持续裁剪）。k25 版首轮 batch8 在 step 3000 OOM（12GB 上限），
batch4 重试运行中。**工程结论：0.5B 级在本卡上 batch≤4，且需更长时间预算。**

### Hamming 预过滤基准（码簿 10.4MB 导出后，CPU numpy）

| 方案 | 全量墙钟 | hit@1 | hit@10 |
|---|---|---|---|
| 全量 dense 余弦（fp32 BLAS） | **0.050s** | 0.349 | 0.842 |
| 全量 packed-LUT Hamming | 1.82s | 0.350 | 0.850 |
| 两段式 M=100（Hamming 粗筛+余弦重排） | 0.236s | 0.350 | 0.835 |
| 两段式 M=500 | 1.77s | 0.350 | 0.841 |

**质量完美转移**：两段式与全量逐位一致。**速度假设被证伪**：朴素 LUT 查表
Hamming 比 BLAS 余弦慢 36 倍（gather 内存瓶颈 vs SIMD 矩阵乘）。真实卖点转为
**存储**：96 字节/条 = fp32 dense 的 1/32，质量零损失；速度持平需 SIMD popcnt
或 GPU popcount 实现（未做）。对 flymemory 的交接口径：位码作为**紧凑地址列**
（省 97% 存储）价值成立；作为**提速手段**需换实现，6k×960 规模下不必。