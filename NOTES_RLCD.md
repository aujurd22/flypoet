# FlyPoet 果蝇机制对照研究笔记（NOTES_RLCD）

> 本文件记录 2026-09-17/18 过夜 FlyPoet v2 实验的方法论、判定标准与全部结果。
> 核心发现摘要见文末"总结论"区。配套报告：REPORT_V2.md。

# 研究笔记：RLCD/Jev 与果蝇决策头的设计交汇（00:40）

## Jev 技术轮廓（社区情报拼图）
- 非自回归并行采样器：一次前向输出全部结构化决策（无逐 token 链）
- 输出 = schema 化选择 + 置信分布 + 置信分（255 选择基数，两阶段处理高基数）
- RLCD 训练：RL 优化"校准决策"——认识论诚实的概率，对照 RLHF（偏好）/RLVR（可验证）
- 定价结构印证：输入收费、输出免费 = 无自回归解码成本

## 与 T59e 误差门控的同源性
多巴胺信号本来就是 RPE（reward prediction error）——校准信号。
RLCD 的"校准置信"目标 = 我们 gatedlearn 误差门控的 RL 化。
差异：RLCD 用外部奖励校准；T59e 的门控是任务内误差信号。融合方案：
第二阶段多域训练加"决策头"（对域选择/答案置信输出分布），
损失 = CE + λ·ECE（期望校准误差）——ECE 项就是"校准决策"的可微化。

## FlyPoet 决策头实验设计（v3 预案）
1. 预训练后冻结主干，接轻量决策头（对 [域, 答案置信] 输出分布）
2. 用 ECE 和 Brier score 评估 std vs flynetS 的校准差异
   预言（两层设计）：flynetS 的 DA 门控历史使其置信更校准
3. 若成立 → "连接组约束产生校准直觉"的定量证据，Jev 的开源生物学对照

## Wikiracing 评测借鉴
可验证、可玩、有传播力。FlyPoet 版：给定古诗词首句，
在 5 个候选续句（1 真 4 扰）中选择——三臂模型的准确率对照。

## 可检验预言（v2 探针升级，00:55）
ECE/Brier 双指标评估两臂置信校准（Brier 免分桶、ECE 看分桶形状）：
- 预言 P-cal：flynetS 的 softmax 置信 ECE ≠ std（方向未定——稀疏化
  可能更诚实也可能更过度自信，但"有差异"本身即证据）
- 评估点：每臂 2000/4000/6000/.../12000 步的探针已含 val loss + erank
  + distinct3；下一版加"首 token 置信 vs 实际频率"的 online 校准曲线
参考：Guo 2017 (ECE), Brier 1950 (proper scoring), ICLR 2025 calibration blog

## 三臂对照的预设判定标准（01:10 锁定，防 HARKing）

数据：std 与 flynetS 同种子同数据同 12k 步（adaptive 臂 6k 另评）。

- **P-样本效率**（主预言）：在 step 2000/4000 匹配点上，
  flynetS val loss < std val loss → 稀疏注意力提升小模型样本效率（T103 复现）。
  判定：两匹配点均成立=强复现；仅一点=弱复现；均不成立=复现失败。
- **P-表征**（T66 预言）：flynetS 激活有效秩（erank）在训练全程
  低于 std 且更平稳（std 方差 > 1.5× flynetS 方差）→ 稀疏化产生选择性表征。
- **P-塌缩警戒**（反预言）：flynetS distinct3 < 0.5 → k-WTA 引发模式塌缩病理，
  记录为机制代价；≥0.7 视为无塌缩。
- **公平性守门**：两臂参数量必须完全相同（92.6M）、吞吐差 <15%、
  同种子——任一不满足则该轮结论降级为"受混杂因素污染"。

明早 REPORT_V2 按此四条逐项打勾，不做事后指标挑选。

## 方法论定位补充（01:20 搜索）
- 我们的 k-WTA = 通道稀疏激活（channel sparsity），区别于 NSA/DSA 的 token 稀疏注意力
- 最近参照：SPDF（Thangarasa 2023，稀疏预训练+稠密微调）——其结论"稀疏预训练等 FLOPs 下可与稠密竞争"
  是我们 fly 臂的外部先验；通道稀疏激活的 SLM 对照文献稀缺 → 本实验有增量价值
- NSA（DeepSeek 2025）证明"原生可训练稀疏注意力"是活跃前沿，但其稀疏在 token 维

## 方法论定位补充（01:20 搜索）
- 我们的 k-WTA = 通道稀疏激活（channel sparsity），区别于 NSA/DSA 的 token 稀疏注意力
- 最近参照：SPDF（Thangarasa 2023，稀疏预训练+稠密微调）——"稀疏预训练等 FLOPs 下可与稠密竞争"
  是 fly 臂的外部先验；通道稀疏激活的 SLM 对照文献稀缺 → 本实验有增量价值
- NSA（DeepSeek 2025）证明"原生可训练稀疏注意力"是活跃前沿，但其稀疏在 token 维

## 第三臂（flynetS_adaptive）判定标准（01:40 锁定，防 HARKing）

数据：adaptive 6k 步（vcvars 环境），与 flynetS 的 0-6k 步段对齐比较。

- **A-1 收敛等价**：adaptive@6k 的 val loss ≤ flynetS@6k val + 0.15
  → 判定"自适应阈值无显著收敛代价"（阈值迭代 ±20% 保留数的余差可接受）
- **A-2 语义保真**：与精确 top-k 保留集重叠 ≥70%（v1 单测实测 86.4%）
  → 忠实于生物自适应机制且功能不偏离
- **A-3 效率**：CUDA kernel 前向 0.030ms vs kthvalue 0.327ms（已证）
  → 训练级 tok/s 验证同量级提升
- **价值定位**：三条通过 → "自适应阈值是精确 top-k 的合法替代且快 11 倍"，
  train_v2 的 flynetS 默认实现切换为 CUDA adaptive

## 附加对照（架构升级收益，白拿的 A/A' 对照）

v1（LayerNorm+GELU+绝对位置）std@4000 步 val=5.390（昨夜数据）
v2.2（RoPE+RMSNorm+SwiGLU+SDPA）std@4000 步 val=待出（今夜数据）
同种子同语料同架构参数预算 → 差值 = 现代块升级的净收益。

## 探针评估设计修正（01:50，std 中期样本审查）
std 生成样本全程为字符乱串——三个因素叠加：
1. probe prompt 仅单字"春"（char-level 无上下文必然发散）
2. 2.2M…更正 53M 字符语料 × 12k 步仍在统计学习早期
3. 生成温度 0.9 偏高
修正（下一版 probe）：prompt 改用语料真实开头 32 字符，
温度 0.7，并附"给定首句续写"与人写对照——明早 REPORT_V2 采用。
erank 76→83 缓升与 loss 下降一致 = 训练本身健康。

## HN 讨论深挖（02:00）——Jev 的本质与争议焦点

### 架构共识（CEO 确认"very accurate"的解读）
- **"换了词表的分类器"**：把 LLM 的 token 词表换成一组"决策"，直接暴露概率
  供程序逻辑使用；本质 = 零样本指令微调的大型分类模型
- 非自回归并行输出 = **语法约束解码的近亲**（OpenAI/Anthropic 的 JSON schema
  接口背后就是约束解码——Jev 的"类型安全"并非全新事物）

### "永不幻觉"宣称的社区批判（对我们最有价值）
- 类型安全 ≠ 事实正确："仍可输出完全错误的合法值"（布尔分类遇到蓝球=被迫幻觉）
- 高置信错误答案就是幻觉——校准性才是真检验标准：
  **1000 个标 0.9 置信的答案应约 900 个正确**（elil17）
- 有价值的正向提议：置信度阈值型 benchmark（"90% 准确率下可自动化多少决策"）

### 对 FlyPoet 的三个直接输入
1. **评测主指标采纳校准性定义**（elil17 标准）：我们 ECE/Brier 评估器
   已实现 → 明早三臂对照正是这个标准的实践
2. **警惕 Jev 式宣称的结构性缺陷**： Forced-choice 无弃权机制的问题
   （bigglebear 的布尔例子）——我们的果蝇决策头若做，应含"弃权/不确定"
   输出维度（生物对应：DA 门控的低置信态）
3. **公平对比的方法论**（WhitneyLand）：给基线同样的约束解码条件——
   与我们 std/flynetS 同预算同数据协议一致

### 社区情绪速写
HN 50+ 楼：一半技术好奇一半 hype 反击；r/LocalLLaMA 有人声称 2 小时复现
（未验证）；The Register 报道标题突出"Doom 演示"。→ 传播学样本：
"非自回归决策模型"的包装成功引发了主流科技社区的结构性辩论。

## 统计显著性补充（02:20）
flynetS vs std 匹配点 val 差（n=6）: +0.0041 ± 0.0195 SE
95% CI [-0.034, +0.042] 含 0 → **k-WTA 无 PPL 代价在统计上成立**
（功效可排除 |真实差异| > 0.042 nats）

## 多 seed 校准对照终版（02:50，修 rng 硬编码后可信数据）

| 臂 | top-1 acc | ECE | gap |
|---|---|---|---|
| std | 0.088±0.006 | 0.0944±0.0039 | — |
| flynetS | **0.333±0.015** | **0.0494±0.0057** | −0.010±0.018 |

- flynetS 校准误差 = std 的一半（跨 seed 稳定）
- flynetS 置信略欠自信（诚实）vs std 过度自信
- flynetS seed 间方差更大 = 稀疏模型对输入更敏感（熟悉度感知机制的另一面）
- **结论升级为统计稳健**：k-WTA 稀疏化产生免费的校准增益
- 方法论教训：rng=0 硬编码 + np.random.seed 顶层覆盖 = 双重陷阱；
  bug 的发现方式是"三个 seed 结果精确相同"——**相同即 bug** 的又一事例

## 三臂多 seed 校准终版（03:30，完整矩阵）

| 臂 | top-1 acc | ECE | Brier | 置信差 gap |
|---|---|---|---|---|
| std | 0.088±0.006 | 0.0944±0.0039 | 0.0872±0.0049 | +0.093±0.005 |
| flynetS 精确 | **0.333±0.015** | 0.0494±0.0057 | 0.1276±0.0017 | −0.010±0.018 |
| flynetS_adaptive CUDA | **0.319±0.012** | **0.0422±0.0101** | 0.1143±0.0031 | +0.003±0.010 |

**三个统计稳健的结论（3 seed × 400 窗口）**：
1. k-WTA 稀疏化（两种实现）top-1 准确率均为 std 的 3 倍以上（跨 seed 稳定）
2. CUDA 自适应阈值的校准误差三臂最低（0.042），且置信差最小（+0.003 近完美校准）
3. 稀疏臂置信略欠自信（负 gap）vs std 过度自信（正 gap）——机制性差异：
   稀疏竞争让高置信预测更谨慎，密集模型盲目的均匀置信

## 熟悉度分层结果（03:30，负结果的诚实记录）

三臂的熟悉度梯度均不显著（|梯度| ≤ 0.019，低于 0.02 阈值）——
"熟悉度感知校准"预言**未获支持**（在此代理定义下）。

可能的解释：
1. 8-gram/bigram 代理对"熟悉度"的度量与模型真实的习得记忆不对齐
   （模型记忆的是 token 共现统计，不是表面 n-gram 重合）
2. 55M 字符语料 × 12k 步 × 1.4 epoch——语料重复度低，
   模型尚未形成"熟悉/陌生"的强区分，分层自然平坦

记录为负结果入报告。未来改进：用训练轮次的 epoch 计数作熟悉度代理
（模型见过的遍数），而非表面 n-gram 重合。

## 决策头实验（04:00）——果蝇表征直接提升决策能力 ★★

任务：8 类文档路由（CWT2 domain 标签，44130 条均衡样本）。
冻结 FlyNet-Dopa 主干，k-WTA 门控后的末层隐状态接 6M 参数决策头（含弃权维度），2000 步短训。

| 主干 | 路由准确率（非弃权） | ECE | 随机基线 |
|---|---|---|---|
| **flynetS_adaptive（果蝇 k-WTA 表征）** | **51.5%** | 0.052 | 12.5% |
| std（普通 transformer 表征） | 39.0% | 0.031 | 12.5% |

**核心发现：果蝇 k-WTA 表征使下游决策准确率提升 +12.5 个百分点**（51.5% vs 39.0%），
且两个决策头的 ECE 都很低（<0.06）——表征校准增益（ECE 0.042 vs 0.094）
传导到了决策层。

这是"果蝇机制→决策价值"的**直接因果链证据**：
同参数、同数据、同训练量，唯一的差异是预训练时有没有稀疏门控——
有门控的表征在没见过的决策任务上泛化更好。
（预判机制：k-WTA 迫使主干学习通道间竞争性的、低冗余的特征，
这种特征对线性可分的决策任务更友好。）

下一步候选：弃权维度激活（Jev 缺陷的补丁）、MCP 包装、0.6B 底座放大。

## 去噪+10k 步版决策头对照（05:20）——机制差异保持 ★★

条件升级：标签去噪（只留 single/multi 一致的清晰样本，47763 训练条）+ 10k 步长训。

| 主干 | 路由准确率（非弃权） | ECE | 随机基线 |
|---|---|---|---|
| **flynetS_adaptive（果蝇 k-WTA）** | **57.5%** | 0.152 | 12.5% |
| std | **39.0%** | 0.031 | 12.5% |

**两个关键结果**：
1. **去噪+长训把果蝇臂推到 57.5%**（vs 模糊标签版 51.5%，+6pp）——
   果蝇臂与 std 的差距从 12.5pp **扩大到 18.5 个百分点**：
   数据越干净、训练越长，k-WTA 表征的泛化优势越明显（与"稀疏特征更好泛化"一致）。
2. **ECE 代价**：长训后 flynetS 决策头过度自信（0.152 vs std 0.031）——
   校准需要温度缩放后处理，或缩短训练。诚实记录：机制带来的准确率优势
   与校准恶化并存，部署时需配温度缩放。

**总结**：k-WTA 表征的决策泛化优势在更严格条件下保持并扩大，
但校准优势不随训练自动保持——需要在推理管线里补温度缩放。

## 长期研究方向确认（03:15，与用户确认）

**放弃**：微调 Qwen3-0.6B 做 Jev 类路由器（市面已有开源实现，重复无意义）
**专注**：验证"生物稀疏门控在预训练时产生天然校准表征"这一假说

核心问题：稀疏竞争（k-WTA）在预训练时是否使模型的置信天然校准——
不需要 RLHF 或事后校准？

### 实验路线图（不急，逐步积累）
1. ✅ 0.1B 三臂对照（已完成：ECE 减半，top-1 3.3 倍）
2. ⬜ 规模扩展：0.6B 底座重复三臂对照（Qwen3-0.6B-Base 已下载）
3. ⬜ 消融：k-WTA 位置（attn vs FFN vs both）× k 比例（5%/10%/25%）
4. ⬜ 多域泛化：中文文学 → 代码 → 多语言
5. ⬜ Reliability diagram + Brier decomposition（分离 refinement/reliability）
6. ⬜ 与 RLCD（Jev）和校准 LLM 文献的定量对比

### 当前证据
- ECE 减半（0.042 vs 0.094），跨 3 seed 稳定
- top-1 准确率 3.3 倍（0.328 vs 0.100）
- PPL 中性（三臂差异 <0.03 nats）
- 无模式塌缩（distinct3 ≥0.71）

## 预注册：0.5B-24k 延长对裁决判据（2026-09-23 01:57 锁定，结果未出）

对照：std_XL24 vs flynetS_XL24k25（478M，b4，24.6M token，24k 步，seed 7）。
已知输入：24k 运行内部 12k 探针 k25 落后 0.057（这是已看到的信息）。
未知输入（本判据针对）：24k 终点差 Δ = dense_24k_final − k25_24k_final。

- Δ > +0.05：**优势随规模×预算增长确认**——"478M 晚期涌现 crossover"坐实，
  0.5B 线升级为主规模线。
- +0.02 < Δ ≤ +0.05：优势保持但与 92.6M-48k（+0.021）同量级——规模效应不可区分于
  噪声，0.5B 线维持辅助地位。
- |Δ| ≤ +0.02：规模中性——216M-b16 反转进一步确认为配置孤例，甜点优势跨规模守恒。
- Δ < −0.05：**优势在大规模+长预算下反转复现**——216M-b16 不是孤例，
  "甜点随规模消失"升级为主叙事，92.6M 结论降级为小模型特例。

无论哪种结果都可发表；判据锁定人：本会话（结果预计 06:45 后产出）。

## 预注册：今夜三实验裁决判据（2026-09-23 21:20 锁定，全部结果未出）

**[D] 216M-b16 反转的种子稳定性**（std_L_s8 vs flynetS_Lk25_s8，b16/49M tok/12k，seed 8）：
- k25 输 ≥0.05：**反转跨 seed 复现**——216M-b16 是真现象（config-locked），
  "规模阶梯干净"表述需加 216M-b16 例外脚注，机制研究立项。
- |差| ≤0.05：反转降级为种子噪声，216M-b16 归档为"一次不稳定测量"。
- k25 赢 ≥0.05：反转是纯噪声，"k25 ≥ dense 于全部匹配点"完全体。

**[A] 0.5B 甜点位置**（flynetS_XLb4k40，12k/b4，锚点：dense 5.4736 / k25 5.5301）：
- k40 val < 5.510（优于 k25 −0.02 以上）：**甜点随规模上移**——scaling law 方向确立。
- 5.510 ≤ k40 ≤ 5.530：甜点 25% 跨 5× 规模稳定。
- k40 > 5.530：40% 过宽，甜点仍在 25%（或更低）。

**[C] 地址簿规模扩展**（store 4k→40k，10×）：
- 检索 hit@10 ≥ 0.75：**Hamming 地址质量随库扩展保持**——flymemory 部署绿灯。
- hit@10 < 0.6：地址质量不随库扩展，flymemory 预过滤方案需要 dense 主导。

## §十五 二时间尺度门控复验（2026-09-26 凌晨，外部论文交叉实验）

来源：Self-Play Pretraining with Zero Data（arXiv 2609.30063）的 reward 消融显示
一步信号（last_step/loss_delta）劣且 seed 双峰，唯有"当前梯度×半程参数位移"的两
时间尺度信号稳定最优。预注册预测：把 FlyPoet 的惊讶门控换成同型两时间尺度信号，
应在 improve 上优于一步 loss 门控；缩短窗口（W8）应退化回一步水平。

实现：cl_experiment.py 新增 gate_type `two_ts`（每 TS_W 步快照参数参考点，
score=|⟨∇L_batch, θ_ref−θ_now⟩|，EMA 阈值同 loss 门控）与 `two_tsc`（余弦归一 +
连续 50 步拒绝强制放行）。FLYPOET_TS_W 环境变量控制窗口。92.6M/std/4 域 CL，
b24/1500 步/域，seed 7/8/9。

结果（improve mean±std，±0.05 判据线）：
- loss 门控 3 seed：0.685±0.073（0.582/0.736/0.737，双峰）
- two_ts W250 3 seed：0.7005±**0.0002**（0.7008/0.7004/0.7003）
- two_ts W8 3 seed：0.5338±**0.0001**（唯一净遗忘臂，forget +0.020×3）
- two_tsc 1 seed：0.719 / skip70 1 seed：0.745

裁决：
- **mean 优势证伪**：W250−loss 门控 = +0.015（within noise）。单 seed 的"+0.12"
  是 s7 低模式抽签假象——与"惊讶选择性之死"同一条纪律的二次应验。
- **稳定性定律幸存**：two_ts 两臂 seed std 1e-4 量级 vs loss 门控 0.073 且双峰。
  门控结构不写在均值上（均值=更新预算主导），写在方差里——一步信号路径依赖级联，
  两时间尺度把窗内数据序噪声积分掉。
- **窗口长度定律 3v3 定案**：W250−W8 = +0.167，W8 三 seed 全部净遗忘。
- **自锁机理（结构性发现）**：train-or-not 形态下，原始内积式必然棘轮锁死
  （不放行→无位移→无信号），pass 率 stage 内单调衰减至 <0.1；论文无此问题因其
  门是"选哪条数据"（always-train）。two_tsc 的保底放行完全消除自锁（pass 稳定
  0.3，improve 0.719，单 seed）。
- 初步：two_ts score 与瞬时 loss 负相关（Spearman −0.62，样本弱）——选"顺"
  不选"难"，与惊讶门控方向相反。

工件：analyze_two_ts.py / aggregate_seeds.py / logs_v2/cl_*_gt-two_ts*；
注意 W8 与 W250 同 seed 同结果文件名会互相覆盖（见 logs_v2/COLLISION_NOTE.txt），
W250 s7=0.7008 与 s9=0.7003 的 result json 已被覆盖，真值在链日志与本节。

## DA 拓扑分析结果（2026-09-23/26，MaleCNS v1.0）

**DA 神经元**：4,447（预测多巴胺能）
- 4,058 Kenyon Cells（KC 本身有 DA 受体！不是只有 DAN 投射 DA）
- 330 DANs（真正的多巴胺神经元）
- 22 CX（中央复合体）
- somaSide: L 2,208 / R 2,236（对称）

**DA 输出**：2.06M 条边
- Top targets：**APL × 2**（前侧对侧神经元，反馈抑制 KC 的关键中间神经元，总权重 221K）
- **MBON01/05/09/11**（蘑菇体输出神经元——每个 compartment 一个 MBON）
- DA→DA 自环：931K 边，180 万总权重——**DA 系统高度自连接**

**FlyPoet 映射**：
- 真实 DA 门控是 **compart化** 的（每个 MBON 对应一个 DA 子群+一个 KC 亚集）
- 我们的全局阈值 = 一个 DA 控制所有 KC = **过度简化**
- 正确实现：**每个参数子室有独立的 μ/K·σ 噪声地板**
- 但随机跳批对照已证 throttling 是主成分——compart 化门控是否优于全局门控是开放问题
